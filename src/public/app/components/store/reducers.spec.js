import reducers from './reducers';

chai.should();

describe('reducers', () => {

  describe('Service', () => {
    // service specs
    it('has a getState property', () => {
      const newState = reducers();
    });
  });

});
